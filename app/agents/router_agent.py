"""Router Agent implementation.

This file defines the Router Agent class, which classifies user/admin types
and query types (text/image), using whitelist checks from the database.
It should entail agent logic, prompts, and tool invocations for routing.
"""
