# Dockerfile for building the app container.

# This file defines the Docker build instructions for the application image.
# It should entail base image, dependency installation, and entrypoint setup.

