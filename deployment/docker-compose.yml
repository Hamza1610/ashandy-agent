# Docker Compose configuration for multi-container setup.

# This file defines services for app, postgres, redis (Pinecone is cloud-managed).
# It should entail service definitions, volumes, ports, and dependencies.

